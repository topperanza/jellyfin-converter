# Default encoding profiles for Jellyfin converter
# Adjust values to match your quality and compatibility needs.

# Recommended CRF values by resolution
CRF_SD=23          # Standard definition (<= 576p)
CRF_720P=21        # HD (720p)
CRF_1080P=20       # Full HD (1080p)
CRF_4K=22          # UHD (2160p)

# x264/x265 preset to balance speed and compression
PRESET=medium      # ultrafast|fast|medium|slow|veryslow

# Preferred video codec
CODEC=h264         # h264 (broad compatibility) or hevc (smaller files, slower)

# Language filtering hints
LANG_PRIORITY=\"eng,ita\"   # Keep English/Italian first; commentary tracks are always preserved.
LANG_FALLBACK=\"rus\"       # Keep Russian only when no other languages exist.
