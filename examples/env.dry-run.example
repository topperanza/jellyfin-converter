# Safe defaults for a first dry run (no writes, no deletions)
# Copy to .env.local (gitignored) or export variables before running.

DRY_RUN=1
DELETE=0
OVERWRITE=0
PARALLEL=1
CODEC=h264
CRF=20
PRESET=medium
HW_ACCEL=none  # avoid GPU probing on first pass
SKIP_DELETE_CONFIRM=0
