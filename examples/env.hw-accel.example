# Hardware-accelerated transcode defaults (safe: no deletion, dry run on)
# Copy to .env.local (gitignored) or export variables before running.

DRY_RUN=1
DELETE=0
OVERWRITE=0
PARALLEL=1
CODEC=hevc
CRF=21
PRESET=medium
HW_ACCEL=nvenc   # set to nvenc|qsv|vaapi|none based on your GPU stack
SKIP_DELETE_CONFIRM=0
